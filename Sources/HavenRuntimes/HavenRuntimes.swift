// HavenRuntimes — runtime adapters (stub)
//
// This module will provide adapters that host RuntimeUnits inside
// concrete execution environments (e.g. Swift Concurrency actors,
// XPC services, containers).
// Implementation is intentionally empty at this stage.

public enum HavenRuntimes {
    // Reserved for runtime adapter protocol + built-in adapters.
}
